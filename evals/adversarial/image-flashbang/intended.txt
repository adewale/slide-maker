Defect: a near-black COVER whose darkness comes from a per-slide `background:` image URL (not a flat token color), cutting straight to a bright cream content slide — a flash-bang for a dark-room audience. deck-lint's flash-bang check only measures flat token/frontmatter colors, so it cannot read the image luminance and sees no jump. Caught by: tools/pixel-audit.mjs on rendered screenshots (or a human watching the cut).
