Defect: the deck keyword-stuffs the literal trigger strings the deterministic contains-style assertions in evals/evals.json look for (build.sh, build pipeline, build system, transition, visual effect, data visualization, guardrail, <v-clicks>, <v-click>) into otherwise content-free slides so every static contains-check goes green while the deck says nothing. Caught by: the held-out memorability / compression / voice criteria in evals/holdout-rubric.md.
