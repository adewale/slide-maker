---
layout: section
transition: iris
---

# Ecosystem

7 presets. 7 scaffold files. 7 build tools. One spec format.

<!-- Section divider for the ecosystem chapter. This section catalogs the infrastructure that surrounds every deck — style presets, scaffold files, build tools, and the deck.spec.md planning format. These are the pieces that make the system reproducible. -->

---
transition: slide-left
---

# One Preset Controls Typography, Color, and Motion

Each of seven presets generates `tokens.css` + `theme.css`.

<v-clicks>

- **editorial-dark** — Playfair Display + Source Sans 3. Near-black bg, cool accent. Serious, high-trust. Fades only.
- **swiss-minimal** — Plus Jakarta Sans + Figtree. White bg, restrained accent. Precise, calm. Two transitions max.
- **bold-modern** — Bebas Neue + DM Sans. Dark bg, bright accent pair. Assertive, energetic. Uses iris and zoom.
- **sumi-e** — Zen Old Mincho + Crimson Pro. Washi paper `#f5f0e8`, sumi ink. Contemplative. Fade is the only breath.
- **tufte-data** — EB Garamond + Source Sans 3. Cream `#fffff8`, data-blue `#2d5f8a`. Scholarly, dense. Sidenote margin layout.
- **cloudflare** — Inter + JetBrains Mono. Warm cream `#fffbf5`, orange `#ff4801`. Workshop-ready. Full transition vocabulary.
- **material-design** — Outfit + Plus Jakarta Sans. M3 surface `#FFFBFE`, primary `#6750A4`. Systematic. M3 easing curves.

</v-clicks>

<!-- Presets are directions, not skins. Each controls typography, color, motion curves, v-click animation style, interaction patterns, and layout tendencies.

[click] editorial-dark — Playfair Display + Source Sans 3. Near-black background, cool accent. Mood is serious, modern, restrained. Transition vocabulary: fade, slide-left, morph-fade. Best for board decks and strategy reviews.

[click] swiss-minimal — Plus Jakarta Sans + Figtree. White or near-white background, one restrained accent. Mood is precise, calm, disciplined. Only 2 transitions allowed. Best for technical briefings and workshops.

[click] bold-modern — Bebas Neue + DM Sans. Dark or saturated background, bright accent pair. Mood is assertive, energetic, launch-oriented. Uses dramatic transitions including iris and zoom-in. Best for launches and internal keynotes.

[click] sumi-e — Zen Old Mincho + Crimson Pro. Washi paper background, sumi ink foreground, vermillion hanko-red used sparingly. Mood is contemplative, spacious, austere. Fade is the only transition — every page turn is a breath. 60-75% negative space per slide.

[click] tufte-data — EB Garamond for display and body, Source Sans 3 for labels. Warm white background, data-blue for charts and annotations, data-red for anomalies. Scholarly, evidence-driven. 60/30 body-sidenote layout. Best for debugging case studies and data analysis.

[click] cloudflare — Inter (Cloudflare's corporate typeface) + JetBrains Mono. Warm cream background, Cloudflare orange accent. Workshop-ready with dark code block islands in a light sea. Full transition vocabulary: slide-left, fade, iris, morph-fade, wipe-right.

[click] material-design — Outfit + Plus Jakarta Sans. M3 baseline purple palette with surface containers at multiple elevation levels. M3 easing: cubic-bezier(0.05, 0.7, 0.1, 1.0). Best for product launches and architecture walkthroughs.

Sources:
- file:slide-maker/STYLE_PRESETS.md — complete preset definitions with palette, typography, motion, and interaction patterns
- file:slide-maker/COMPILER_RULES.md — v-click animation presets per style preset -->

---
transition: slide-left
---

# Every Deck Ships With Seven Scaffold Files

Generated by `new-deck.sh`, present in every deck.

<v-clicks>

- **setup/shortcuts.ts** — Registers `?` (help overlay) and `p` (presenter mode) keyboard shortcuts
- **composables/useHelp.ts** — Reactive `showHelp` state shared between shortcuts and the help component
- **components/KeyboardHelp.vue** — Full-screen shortcut overlay using `--deck-accent` and `--deck-font-*` tokens
- **global-top.vue** — Mounts KeyboardHelp when `showHelp` is true
- **global-bottom.vue** — Footer with slide number and title, hidden on cover/end layouts
- **setup/mermaid-renderer.ts** — Beautiful Mermaid rendering with deck palette tokens injected automatically
- **styles/transitions.css** — 12 cinematic CSS transitions imported by `styles/index.css`

</v-clicks>

<!-- The universal scaffold ensures every deck ships with keyboard navigation, presenter support, Mermaid rendering, and cinematic transitions. These files are generated once by new-deck.sh and should never be removed.

[click] setup/shortcuts.ts — registers two keyboard shortcuts. Question mark toggles the help overlay, p opens presenter mode.

[click] composables/useHelp.ts — reactive state that bridges shortcuts and the help component. A single shared ref.

[click] components/KeyboardHelp.vue — the full-screen overlay that shows all keyboard shortcuts. Uses --deck-accent and --deck-font-* tokens so it matches every preset.

[click] global-top.vue — Slidev's global-top layer renders above slide content. Mounts KeyboardHelp conditionally. Demo's version is a superset that includes a laser pointer.

[click] global-bottom.vue — Slidev's global-bottom layer. Shows slide number + deck title in a footer. Uses $nav.currentLayout to hide on cover and end slides.

[click] setup/mermaid-renderer.ts — registers Beautiful Mermaid as the renderer. Reads --deck-bg, --deck-fg, --deck-accent, and --deck-muted from computed styles at render time. Requires Slidev v52+.

[click] styles/transitions.css — 12 cinematic transitions: iris, wipe-right, wipe-up, morph-fade, zoom-in, zoom-out, flip-x, flip-y, cube, swing, blur, glide. All pure CSS, no JavaScript animation libraries.

Sources:
- file:slide-maker/COMPILER_RULES.md — universal features specification and scaffold file list
- file:tools/new-deck.sh — scaffold generation script -->

---
transition: slide-left
---

# Seven Scripts Catch Problems Before the Audience Does

Scaffolding, validation, and comparison.

<v-clicks>

- **new-deck.sh** — Scaffold a complete deck from any preset with all 7 scaffold files
- **deck-lint.mjs** — Validate structure, overflow, sources, and quality against compiler rules
- **style-audit.mjs** — Audit CSS token usage across decks for hardcoded hex values
- **deck-preview.mjs** — Quick text preview of slide content without launching the dev server
- **deck-diff.mjs** — Diff two versions of a deck to show what changed between iterations
- **compare-decks.mjs** — Side-by-side comparison of multiple decks for inconsistencies
- **build-and-verify.sh** — Full build pipeline with post-build checks as the final gate

</v-clicks>

<!-- Seven scripts in tools/ handle scaffolding, validation, and comparison.

[click] new-deck.sh — scaffolds a complete deck from any preset. Generates all 7 scaffold files plus styles. The starting point for every new deck.

[click] deck-lint.mjs — validates structure, overflow, sources, and quality. Checks the 7-bullet, 8-code-line, and 60-character-bullet overflow guards. Flags missing source citations and Mermaid annotation issues.

[click] style-audit.mjs — audits CSS token usage. Verifies no hardcoded hex values appear where --deck-* tokens should be. Catches palette drift.

[click] deck-preview.mjs — quick text preview of slide content. Useful for reviewing narrative flow without launching the Slidev dev server.

[click] deck-diff.mjs — diffs two versions of a deck. Shows slide-level changes between iterations so reviewers know what moved.

[click] compare-decks.mjs — side-by-side comparison of multiple decks. Spots structural inconsistencies across the collection, like differing scaffold files.

[click] build-and-verify.sh — full build pipeline with post-build checks. The final gate before deployment. Runs the build and then verifies the output.

Sources:
- file:tools/deck-lint.mjs — structural validator with source citation checks
- file:tools/new-deck.sh — deck scaffolding script
- file:slide-maker/COMPILER_RULES.md — acceptance checklist that deck-lint enforces -->

---
transition: slide-up
---

# deck.spec.md Is the Planning Schema

Seven sections define intent before a single slide is written.

<v-clicks>

- **Meta** — title, purpose, audience, tone, target-length, style-preset
- **Source Materials** — documents digested to produce the deck (project decks only)
- **Through-Line** — the conceptual thread: type, concept, and which slides it surfaces in
- **Design Tokens** — colors, typography, spacing, motion as semantic variables
- **Layout System** — preferred builtins, custom layouts, components, CSS files
- **Slides** — per-slide kind, layout, title, body, notes, and source citations
- **Notes Policy** — optional presenter notes guidelines and asset rules

</v-clicks>

<!-- deck.spec.md is the planning schema for a Slidev deck project. It captures intent, not implementation. The spec must be human-readable and deterministic enough to compile into a working deck.

[click] Meta — required fields: title, purpose, audience, tone, target-length, style-preset. Optional: project-url (triggers source gathering and project color extraction), subtitle, author, date.

[click] Source Materials — project decks only. Lists at least 2 documents digested to produce the deck: readme, changelog, architecture, lessons-learned, screenshots, specs, research. Each with a path and parenthetical summary.

[click] Through-Line — the conceptual thread. Types: metaphor, question, concept, provocation, design-rule. Must appear in at least 3 slides, ideally 5-6, gaining new meaning each time. Not a tagline — an analytical instrument.

[click] Design Tokens — semantic variables for colors (bg, fg, accent, muted), typography (display, body, mono), and motion (preset name). Keep token count low. Avoid slide-specific tokens.

[click] Layout System — prefer-builtins: true. Lists which builtins, custom layouts, components, and CSS files the deck needs. Every listed custom layout must be used. Every listed component must be justified.

[click] Slides — each is a ### Slide N block. Required: kind, layout, title. Optional: subtitle, body, left, right, media, image, alt, notes, sources, component, props. No giant HTML blobs.

[click] Notes Policy and Update History are optional sections for governing presenter notes and tracking spec revisions.

Sources:
- file:slide-maker/DECK_SPEC.md — complete deck.spec.md specification with canonical template
- file:slide-maker/COMPILER_RULES.md — how deck.spec.md compiles into a Slidev project -->

---
layout: fact
transition: fade
---

# 5 + 8 + 13 + 7 + 7

5 visual effects. 8 data viz. 13 transitions. 7 presets. 7 tools.

<!-- The component catalog: 5 visual effect components (GlassCard, ImageFX, RevealPath, ShadowStack, CornerCard) for visual polish, plus 8 data visualization components (Sparkline, SmallMultiples, DataTable, MicroBar, SlopeChart, BulletBar, DotStrip, WinLoss) for inline data presentation. 13 cinematic CSS transitions with semantic meanings. 7 style presets generating complete visual systems from tokens through typography to motion. 7 build tools for scaffolding, validation, and comparison.

Sources:
- file:slide-maker/COMPILER_RULES.md — complete component and tool inventory
- file:slide-maker/STYLE_PRESETS.md — seven preset definitions -->
